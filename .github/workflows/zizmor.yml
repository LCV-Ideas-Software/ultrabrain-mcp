name: Zizmor

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "43 5 * * 1"
  workflow_dispatch:

permissions: write-all

concurrency:
  group: zizmor-${{ github.ref }}
  queue: max
  cancel-in-progress: false

jobs:
  zizmor:
    name: Run zizmor
    permissions: write-all
    uses: LCV-Ideas-Software/.github/.github/workflows/zizmor.yml@f696635ed718dfb1b9576a1e9bcb1173cc278901 # v1.0.1
