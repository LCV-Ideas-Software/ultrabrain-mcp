# This workflow is managed by gh actions-lock.

name: Linear Release
on:
  push:
    branches:
      - main
# Política enterprise (Discussion #150): sem write-all. Conjunto vazio no escopo do
# workflow e grant mínimo declarado no job.
permissions: {}
# Fila integral: o grupo serializa as criações de release e queue: max preserva
# todos os runs pendentes suportados, sem substituir pushes intermediários.
concurrency:
  group: linear-release-${{ github.ref }}
  queue: max
jobs:
  linear_release:
    name: Linear Release
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Fail-closed: falha de checkout, environment, timeout ou Linear Release
    # deixa o run vermelho e impede que a divergência seja projetada como sucesso.
    # Política zizmor da casa: secret só é lido dentro de environment dedicado.
    # LINEAR_ACCESS_KEY vive como environment secret de linear-release — escopo
    # restrito a criar releases na pipeline deste repositório, nada além.
    environment: linear-release
    permissions:
      contents: read # o CLI lê o histórico de commits para associar issues à release
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0 # histórico completo: o CLI varre commits desde a última release
          persist-credentials: false

      # Action oficial da Linear, pinada pelo SHA da release v0.16.0. O CLI fica
      # explicitamente selecionado na mesma versão. O instalador oficial ainda
      # baixa o CLI sem autenticação criptográfica dos bytes; essa lacuna upstream
      # está rastreada em linear/linear-release-action#59.
      - name: Create Linear release with the official action
        id: linear_release
        uses: linear/linear-release-action@0a25abab892a91062ebf42260dbb2ce6277aa205 # v0.16.0
        with:
          access_key: ${{ secrets.LINEAR_ACCESS_KEY }}
          cli_version: v0.16.0
